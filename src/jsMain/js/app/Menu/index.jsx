import React from 'react';
import ColorPicker from './components/ColorPicker';

const Menu = (props) => {
  return (
    <div className="react-menu--wrapper">
      <ColorPicker />
    </div>
  );
};

export default Menu;
